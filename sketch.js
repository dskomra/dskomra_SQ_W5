function preload() {
  let background = loadImage(assets / images / background.png);
  let mario = loadImage(assets / images / mario.png);
  let coin = loadImage(assets / images / coin.png);
}

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
}
